<?xml version="1.0"?>
<launch>

  <!-- - - - - - - - - - - - - - - - - - - - - - - -->
  <!-- ROS head file to simulate vizzy in gazebo   -->
  <!-- - - - - - - - - - - - - - - - - - - - - - - -->

  <!-- robot configuration parameters -->
	<arg name="robot" value="vizzy"/> <!-- full, lower_body, foot -->

  <!-- simulation parameters and robot configuration parameters -->
  <arg name="pose" value="-x 0.0 -y 0.0 -z 0.2 -R 0.0 -P 0.0 -Y 1.57"/>
  <arg name="paused" value="false"/>
  <arg name="use_sim_time" value="true"/>
  <arg name="gui" value="true"/>
  <arg name="world" value="vizzy_test_world.world" />
  <arg name="yarp" value="false"/>

	<!-- Launch robot in gazebo simulator -->
  <include file="$(find vizzy_gazebo)/launch/gazebo.launch">
    <arg name="robot" default="$(arg robot)"/>
    <arg name="pose" value="$(arg pose)"/>
    <arg name="paused" value="$(arg paused)"/>
  	<arg name="use_sim_time" value="$(arg use_sim_time)" />
  	<arg name="gui" default="$(arg gui)" />
  	<arg name="world" default="$(arg world)" />
  </include>

	<!-- Bringup robot controllers -->
  <include file="$(find vizzy_control)/launch/vizzy_control.launch">

  </include>

	<!-- Launch navigation stuff -->


	<!-- Launch rviz -->
 <node name="rviz" pkg="rviz" type="rviz" args="-d $(find vizzy_launch)/rviz/vizzy.rviz" />
</launch>
